# Papyrs

## Instruction

To deploy locally, do the following:

```bash
git clone https://github.com/peterpeterparker/papyrs-wip
cd papyrs-wip
npm ci
juno dev start
juno login --emulator --mode development
juno config --mode development
npm run dev
```